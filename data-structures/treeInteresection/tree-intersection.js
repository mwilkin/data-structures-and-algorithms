'use strict';
const BinarySearchTree = require('../tree/tree.js');

const treeIntersection = (tree1, tree2) => {
  if(!tree1 || !tree2) {throw new Error('Invalid input: treeIntersection() requires two trees');}

  let results = [];


};

module.exports = treeIntersection;
